/// Names of the Hive boxes opened at startup (see `main.dart`). Centralised
/// here so a box name is never typed as a raw string in more than one place.
abstract final class HiveBoxes {
  static const tasbeeh = 'tasbeeh';
  static const customDhikr = 'custom_dhikr';
}
